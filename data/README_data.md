# Data folder

This folder is kept for project consistency, but V2 does not use real external datasets, JSON scenario files, or live inputs.

In V2, all environment behaviour is defined directly in Python code.

If the project reaches V3, this folder may later store structured external inputs.
